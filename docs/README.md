# docs/

| File | Read it when |
|---|---|
| `ARCHITECTURE.md` | You need the shape of the system and why the router is deterministic |
| `WHATSAPP_COMMANDS.md` | You are the owner, or you are changing a command |
| `OPERATIONS.md` | Installing on the Ubuntu PC, running it as a service, backups, recovery |

The authority documents are in `../directives/`. Where these docs and a
directive disagree, the directive is right and the docs are stale.
