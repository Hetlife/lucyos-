"""Repo-owned Nebula native client source.

The deployable Nebula copy remains flat (``client.py`` next to ``ui.py``);
this package exists so LucyOS tests can import the same source without opening
a device or changing the live laptop service.
"""
